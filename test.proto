syntax = "proto3";

message value
{
    int64 aNum = 1;
    bool check = 2;
    string aha = 3;
}

message chat
{
    string data = 1;
}

service main
{
    rpc aMethod(value) returns (value){}
    rpc bidirectional(stream chat) returns (stream chat){}
}